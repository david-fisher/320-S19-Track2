{
  "name": "Members Only",
  "description": "The Members Only website.",
  "image": "heroku/python",
  "repository": "https://github.com/david-fisher/320-S19-Track2",
  "keywords": ["python", "django" ],
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "postdeploy": "npm install --dev; npm run dev",
        "test-setup": "python manage.py collectstatic --noinput",
        "test": "python manage.py test"
      }
    }
  }
}
